# Variables para `docker-compose.yml` (despliegue en fisgon.prasoft.es).
# Copia este archivo a .env (no versionado) y rellena los valores reales.

OPENCODE_API_KEY=
OPENCODE_MODEL=deepseek-v4-flash
FISGON_JWT_SECRET=
FISGON_INTERESTING_THRESHOLD=6
FISGON_POLL_MINUTES=20
